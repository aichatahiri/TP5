<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE hibernate-configuration PUBLIC
        "-//Hibernate/Hibernate Configuration DTD 3.0//EN"
        "http://hibernate.sourceforge.net/hibernate-configuration-3.0.dtd">

<hibernate-configuration>
    <session-factory>

        <!-- ✅ Connexion à MySQL -->
        <property name="hibernate.connection.driver_class">com.mysql.cj.jdbc.Driver</property>
        <property name="hibernate.connection.url">
            jdbc:mysql://localhost:3306/base?createDatabaseIfNotExist=true&amp;useSSL=false&amp;serverTimezone=UTC
        </property>
        <property name="hibernate.connection.username">root</property>
        <property name="hibernate.connection.password"></property>

        <!-- ✅ Dialecte Hibernate moderne -->
        <property name="hibernate.dialect">org.hibernate.dialect.MySQL8Dialect</property>

        <!-- ✅ Options utiles -->
        <property name="hibernate.show_sql">true</property>
        <property name="hibernate.format_sql">true</property>

        <!-- ✅ Stratégie de création de tables -->
        <property name="hibernate.hbm2ddl.auto">update</property>

        <!-- ✅ Mapping des entités -->
        <mapping class="entities.Salle"/>
        <mapping class="entities.Machine"/>
        <mapping class="entities.Category"/>
        <mapping class="entities.Product"/>

    </session-factory>
</hibernate-configuration>
